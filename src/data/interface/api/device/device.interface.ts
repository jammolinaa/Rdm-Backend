export interface IDevice {
  device_id: number;
  name: string;
  source_id: number;
  system_device_id: number;
  is_active: boolean;
  comunication_route: string;
  event: string;
  user_id: number;
}
